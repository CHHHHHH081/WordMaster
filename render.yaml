services:
  - type: web
    name: vocab-platform
    env: node
    buildCommand: npm install && npx prisma generate && npx next build
    startCommand: npx next start -p $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
